upstream tibava_backend_gunicorn {
    server backend:8000 fail_timeout=0;
}


server {
    # use 'listen 80 deferred;' for Linux
    # use 'listen 80 accept_filter=httpready;' for FreeBSD
    listen 80;
    client_max_body_size 50G;
    root /app/frontend/dist/;

    # set the correct host(s) for your site
    server_name localhost;

    keepalive_timeout 5;


    location {{ SCRIPT_NAME }}/backend/ {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $http_host;
        proxy_redirect off;

        # Match your upstream definition explicitly
        proxy_pass http://tibava_backend_gunicorn;
    }

    location {{ SCRIPT_NAME }}/media/ {
        alias /data/media/;
    }

    location {{ SCRIPT_NAME }}/thumbnails/ {
        alias /data/predictions/;
    }

    location {{ SCRIPT_NAME }}/css/ {
       alias /app/frontend/dist/css/;
    }

    location {{ SCRIPT_NAME }}/js/ {
       alias /app/frontend/dist/js/;
    }

    location {{ SCRIPT_NAME }}/fonts/ {
       alias /app/frontend/dist/fonts/;
    }

    location {{ SCRIPT_NAME }}/img/ {
       alias /app/frontend/dist/img/;
    }

    location {{ SCRIPT_NAME }} {
        index  index.html;
        try_files $uri $uri/ /index.html;
    }


}